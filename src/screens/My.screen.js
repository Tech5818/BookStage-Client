import { Profile } from "../components/my/Profile";

const MyScreen = () => {
    // 이름: 기본적으로 GUEST임 수정 가능
    // 이메일 있음
    // 프로필 이미지가 원으로 되어 있음
    // 회원 탈퇴 버튼
    return(
        <Profile />
    )
}

export default MyScreen;